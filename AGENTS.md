# AGENTS.md

Campus Forge: a campus-wide collectible MTG game. **Supabase is the backend** (Postgres + RLS + RPC + Auth + Storage + Edge Functions + Realtime); the **Vite/React PWA on GitHub Pages** is the client and talks to Supabase directly; a standalone **`battle-engine/`** Spring Boot service wraps the Forge headless rules engine and is exposed from a campus PC through a Cloudflare quick tunnel. The pre-Supabase Spring backend was deleted (see git history before `2c195d4` if you need its semantics).

## Current state (2026-09-24)
- **Hosted Supabase** (`prjsiywvhxqnsvsmfgxm`): migrations **00–15 applied** (13 security hardening, 14 starter pack + backfill, 15 `app_config`, all applied via the Management API). Edge Functions `claim`, `qr-catalog` and `admin-spawn` are **deployed**. `QR_SIGNING_SECRET` is set to a project-specific value (not the old committed default). 0 claims printed so far.
- **Pages**: https://igdaiiitd.github.io/TheFury/ is deployed from `main` and the console is clean. Static routes return 200 via route shells; the battle transport is native WebSocket, not SockJS.
- **Battle engine**: builds from a fresh clone and runs via `battle-engine\battle-server.ps1 install`, a SYSTEM scheduled task. **Open:** that install must be run (and verified) from an **admin** shell on the host PC; until then battles are offline and the Battle tab stays hidden.
- **Open (owner):** promote an admin (`update public.profiles set role='ADMIN' where email=…`), and revoke the Supabase PAT that was shared in chat (it lives in the gitignored root `.env` as `SUPABASE_ACCESS_TOKEN`).

## Layout
- `web-client/` — Vite + React 18 + TS PWA; `src/api/*` is the only layer that talks to Supabase. Deployed to GitHub Pages under `/TheFury/`.
- `supabase/` — `migrations/*.sql` (schema source of truth), `functions/` (Edge Functions), `seed.sql` (base catalog, 104 cards), `seed_sets/*.sql` (imported sets, **generated** by `scripts/import-scryfall-set.mjs`, applied after `seed.sql`; currently M19 = 253 cards), `SQL_EDITOR_SETUP.sql` (**generated**: the one-paste bootstrap for a fresh project), `tests/` (PGlite schema suite + the generator).
- `battle-engine/` — Spring Boot 3.3 service (port **17175**) embedding `forge-headless`. In-memory match state; validates Supabase JWTs via JWKS; writes via service-role RPCs. Setup/deploy guide: `battle-engine/README.md`. Scripts:
  - `setup-forge.ps1` — recreates the gitignored `forge-engine/` checkout (upstream Forge `fd8196a8`, blobless + sparse, `core.eol=lf`), applies `forge/campusforge-forge.patch`, copies `forge/forge-headless`, runs `mvn install`. Never edit `forge-engine/` directly; change `battle-engine/forge/` and re-run the script.
  - `run-engine.ps1` — runs the jar with `SUPABASE_*` read from the root `.env`; CORS allows the Pages origin + localhost.
  - `start-public.ps1` — engine + `cloudflared` quick tunnel. It publishes the random `*.trycloudflare.com` URL to `app_config.battle_engine_url`, watches both processes, and clears the URL on exit.
  - `battle-server.ps1 install|start|stop|restart|update|status|logs|uninstall` — admin service wrapper: a SYSTEM task at boot running a supervisor loop around `start-public.ps1`. **`update` = stop → `mvn clean package` (tests) → start; use it to deploy engine changes** (the running JVM locks the jar). From a non-admin shell, `status` can't see the SYSTEM task or processes and says so. Logs go to `battle-engine/logs/` (gitignored).
- `admin-console/` — Vite admin app on Supabase, **not deployed** (`npm run dev`, port 17173).
- `scripts/import-scryfall-set.mjs <set> [--upload-art]` — imports a whole set: Scryfall data, only cards Forge implements, rarity→ownership (common UNLIMITED, uncommon/rare UNLOCK, mythic UNIQUE), writes `supabase/seed_sets/<set>.sql`, optionally uploads art. Card data belongs in seed files, **not migrations** (migrations run before `seed.sql` on a fresh install, which would create duplicate names). Procedure: `DOCS/operations.md#add-a-card-set`.
- `scripts/download-card-art.ps1` — Scryfall art download + upload for the base catalog.
- **Docs: `DOCS/`** is the single documentation home: architecture, game rules, database, Edge Functions, QR codes, frontend, battle engine, operations runbook, security and testing. `DOCS/design/original-plan.md` is the historical v2 plan (pre-Supabase). The root `README.md` and `battle-engine/README.md` are short entry points into `DOCS/`. **When behaviour changes, update the matching `DOCS/` page in the same commit.**

## Commands
- Node v20, Deno, Java 21, `supabase` CLI 2.x and `cloudflared` (Chocolatey: `C:\ProgramData\chocolatey\bin`) are available. **Maven is NOT on PATH**: use `C:\Users\student\AppData\Local\Temp\opencode\apache-maven-3.9.9\bin\mvn.cmd` (prepend it to `$env:Path`) and run from the module dir. winget has been flaky; prefer choco.
- web-client (from `web-client/`): `npm run lint` = `tsc --noEmit` (there is no ESLint); `npm test` = `vitest run`; `npm run build` = `tsc -b && vite build`. **After editing frontend code, run `npm run lint` AND `npm test`.** Focused: `npm test -- src/pages/LoginPage.test.tsx` (or `-t "<name>"`).
- Pages build: `BASE_PATH=/TheFury/ npm run build` (Vite `base` = `BASE_PATH` env, default `/`).
- Supabase schema tests (from `supabase/tests/`): `npm ci && npm test`. This applies every migration + seed to PGlite behind `shim.sql` (Supabase roles, `auth.uid()`, default grants), checks RLS/privileges/RPC flows, and fails if `SQL_EDITOR_SETUP.sql` is stale. **After adding a migration: add checks to `schema.test.mjs`, then `npm run build:setup-sql`.**
- Edge Functions: `deno test --allow-env supabase/functions/_shared/qr.test.ts`; `deno check supabase/functions/<name>/index.ts`. Deploy: `supabase functions deploy claim qr-catalog admin-spawn --project-ref prjsiywvhxqnsvsmfgxm --use-api`, with `SUPABASE_ACCESS_TOKEN` exported from `.env` (no Docker needed).
- battle-engine: run `setup-forge.ps1 -Mvn <mvn.cmd>` once, then `mvn clean package` in `battle-engine/` (this also runs the JUnit tests). If `clean` fails with "Failed to delete …jar", an engine JVM is still running; `battle-server.ps1 stop` it first.
- Apply SQL to hosted: `POST https://api.supabase.com/v1/projects/prjsiywvhxqnsvsmfgxm/database/query` with `{"query": <file contents>}` and PAT auth (multi-statement + `do $$` bodies are fine). Verify afterwards through PostgREST with the publishable/service key.

## Supabase
- Hosted project ref `prjsiywvhxqnsvsmfgxm` (URL + publishable key in `web-client/.env.development`; **public by design**). The root `.env` (gitignored) holds `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` and `SUPABASE_ACCESS_TOKEN`.
- **Schema changes = a new file in `supabase/migrations/`; never hand-edit schema via the dashboard.** Migrations 00–10 use plain `CREATE` (not re-runnable); **11+ must be idempotent** (the test suite re-runs them).
- **The hosted DB has no `supabase_migrations` table** (it was provisioned by pasting `SQL_EDITOR_SETUP.sql`), so **`supabase db push` is unusable**. Apply new migration files via the SQL Editor or the Management API (see Commands).
- **Function privileges are deny-by-default (migration 13).** Supabase grants EXECUTE on new public functions to `anon`/`authenticated` *directly*, so `revoke ... from public` alone does nothing. Every new function must end with an explicit `revoke execute ... from public, anon, authenticated;` plus `grant execute ... to <role>`. Client-callable RPCs must derive the actor from `auth.uid()` and reject NULL.
- `profiles`: players may only change display name, avatar, student id, cohort, onboarding and last_login (`trg_profiles_guard`); role/XP/level/banned change only via admins or definer functions.
- Players have **no direct write** on unlocks, discoveries, achievements, trades, trade_cards or game_log. Those go through `apply_claim` (service role, via the `claim` EF), the trade RPCs, `grant_starter_pack` and `record_match_result`.
- `game_log` = durable append-only history (CLAIM/TRADE/MATCH/STARTER/SPAWN). `activity_feed` = 50-row UI ring (signed-in only). Realtime publication: `activity_feed`, `trades`, `app_config`. `app_config` is public key/value runtime config (anon-readable; service role/admins write); never put secrets in it.
- Edge Functions `claim`, `qr-catalog`, `admin-spawn` (+ `_shared/qr.ts`, `_shared/http.ts`). **Every response needs `CORS_HEADERS`** (use the `_shared/http.ts` helpers), or browsers on Pages drop it. `QR_SIGNING_SECRET` (≥32 chars) is a required project secret with no fallback; the functions return 500 without it (and 401 before auth if it's fine).
- Auth: email/password, `mailer_autoconfirm` on. `profiles` is 1:1 with `auth.users` via `handle_new_user`, which also stores a valid cohort from signup metadata and calls `grant_starter_pack`.
- Card art: public Storage bucket `card-art` (`{slug}.jpg`); `src/lib/scryfall.tsx` returns the storage URL when `VITE_SUPABASE_URL` is set, else local `/card-art/`.

## Frontend / Pages gotchas
- **The router basename must come from Vite's base** (`main.tsx`: `import.meta.env.BASE_URL`). Omit it and the Pages subpath renders a blank page with no console error.
- **Deep links:** Pages has no SPA rewrites. The `staticRouteShells` plugin in `vite.config.ts` copies `index.html` to `<route>.html` and `<route>/index.html` for every static `<Route path>` in `App.tsx` (parsed from source), so those URLs return 200. The workflow's `404.html` copy only catches unknown paths. Routes with params (`:id`/`*`) would need the 404 fallback; prefer query strings.
- Env: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_BATTLE_ENGINE_URL`. A non-empty build-time `VITE_BATTLE_ENGINE_URL` wins (local dev). When it's empty (the Pages build), `api/battleEngineDiscovery.ts` reads `app_config.battle_engine_url` at startup, probes `/api/v1/battle/features`, follows Realtime updates and re-renders. Until then `battleEngineConfigured()` is false and the Battle tab/route is hidden. `battleEndpoints.ts` resolves the base URL per request for this reason.
- Battle live updates use `@stomp/stompjs` with **`brokerURL: battleWsUrl()` (native WebSocket, `wss://` in prod)**. Don't reintroduce SockJS: it rejects ws/wss URLs and its `unload` listener triggers a Permissions-Policy violation.
- `src/api/*` is the only Supabase layer: `supabaseClient.ts` (client + `callEdgeFunction`), `endpoints.ts` (PostgREST/RPC), `tradeEndpoints.ts` (incl. `subscribeToTrades`), `qrEndpoints.ts`, `feedRealtime.ts`, `battleEngineDiscovery.ts`. Keep exported signatures stable: tests `vi.mock` these modules, so add new exports to the mocks.
- No dev-server proxy: Supabase and the engine are called cross-origin. `src/lib/idb.ts` no-ops when IndexedDB is absent, so jsdom needs no IDB mock. Keep `jsdom` pinned `^25` with `vitest ^0.34`.
- Pages deploy = `.github/workflows/deploy-web.yml` (lint + test + build, then copies `index.html` → `404.html`). Settings → Pages **Source must be "GitHub Actions"**, and the `github-pages` environment branch policy must allow `main`. `supabase-tests.yml` runs the schema + Deno tests on `supabase/**` changes.

## Battle engine / hosting gotchas
- The host PC's "static IP" (`192.168.194.106`) is a private campus address behind a shared NAT (`103.25.231.106`); nothing inbound works. Outbound TCP 7844 (Cloudflare tunnel) and 443 are open. Quick tunnels need no account or domain, but their **URL changes every start**, hence `app_config` + runtime discovery. Tailscale Funnel login failed on this PC; a named Cloudflare Tunnel needs a domain.
- STOMP transport is **native WebSocket only** at `/ws/match` (the SockJS endpoint was removed). CONNECT requires `Authorization: Bearer <supabase access token>`; `/api/v1/battle/features` is the only unauthenticated REST route (used as the health probe).
- `SUPABASE_JWT_SECRET` defaults to empty. The HS256 fallback is used only if it's set; never restore the old CLI default secret.
- `GlobalExceptionHandler` must keep Spring MVC `ErrorResponse` statuses (404/405/400); don't let the catch-all turn them into 500s.
- Lobby join: `activateMatch` is a conditional PATCH (`&status=eq.WAITING`), so exactly one joiner wins. A repeated join by the player who already joined returns the running match (200), not 409. The PWA disables lobby buttons while a request is in flight and shows the engine's `{code, message}` error text.
- **Processes started from an agent/IDE session die with that session.** For anything long-running, use `battle-server.ps1 install` (admin), not a background shell.
- PowerShell 5.1 pitfalls hit here: variables are **case-insensitive** (`$action` clobbered the `[ValidateSet] $Action` param); redirected native stderr under `$ErrorActionPreference='Stop'` throws; `>>` writes UTF-16; embedded quotes in native args get mangled (use `Start-Process -ArgumentList` arrays); `Content-Type` must go in `-ContentType`, not headers.
- The Forge repo is `* text=auto`; checkouts must use `core.eol=lf` or `git apply` of the patch fails. Windows long paths need `core.longpaths` (the sparse checkout avoids the GUI modules).

## Domain invariants (single-sourced in SQL/EF — don't duplicate)
- Level: Postgres `compute_level(xp)` (`level = 1 + xp/100`) is the only formula.
- Cohorts: `is_cohort_valid()` / `department_of()` (B.Tech CSE/CSAI/CSAM/CSB/CSSS/CSD/CSECON/ECE/EVE; M.Tech CSE/ECE).
- QR tokens: signed `V1.<CORE>.<SIG>` (HMAC-SHA256 hex, constant-time); forged or tampered tokens → **400 before any DB lookup**. Print-catalog cores are deterministic from the card id (`deterministicCore` in TS == `card_print_core` in SQL) and therefore **must** be signed. Unsigned 12-char codes are accepted only for admin-spawned claims (`claims.spawned_by` set). The QR image encodes the full token; `qr-catalog` JSON is `[{cardName, qrContent}]`.
- Ownership: an UNLOCK unlock = 1 copy; UNLIMITED (basic lands, the 10 starter creatures, imported-set commons) = everyone owns infinite copies; UNIQUE = one serialized `unique_cards` row each. Format rules (STANDARD max 4 per non-basic) still apply in decks.
- Starter creatures (UNLIMITED): Raging Goblin, Goblin Piker, Vulshok Berserker, Hill Giant, Fire Elemental, Grizzly Bears, Elvish Warrior, Trained Armodon, War Mammoth, Craw Wurm. `grant_starter_pack` gives each new player a 60-card "Red-Green Starter" deck (4x each + 10 Mountain + 10 Forest), once (STARTER row in `game_log`).
- Trades: `accept_trade` locks the trade row + all `unique_cards` in **sorted-UUID order**, re-verifies ownership, swaps owners, appends to `unique_cards.history`; 24h TTL → 410 on expired.
- Admin = `profiles.role = 'ADMIN'` (RLS policies + EF gates). Banned players cannot claim or trade.
- Forge never touches Postgres; battle-engine keeps match state in memory only (a restart drops live matches). `record_match_result` awards 50 XP × event bonus and writes MATCH rows; the winner must be a participant.

## Battle UI (frontend)
- `src/pages/battleUi.ts` is the pure, tested logic layer. Instant-speed spells are demoted outside `COMBAT_*` phases; the ActionBar confirm row appears only when ≥1 card is selected. `CostPartMana.canPay()` always returns true (engine TODO; real filtering is in `HumanPlayerController.getPlayableSAs()`).
